# Backend Node legado

Este diretório preserva o backend Node antigo que antes ficava em `backend/`.

Ele foi removido do caminho de deploy principal para evitar autodetecção incorreta de stack no Railway. O backend oficial do projeto para produção é a API FastAPI em `backend/`.
